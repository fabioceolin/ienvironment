export enum Role {
  Administrator = 0,
  User = 1,
}

export enum EntityType {
  String = 0,
  Int = 1,
  Boolean = 2,
  Numeric = 3,
}

export enum DayOfWeek {
  Sunday = 0,
  Monday = 1,
  Tuesday = 2,
  Wednesday = 3,
  Thursday = 4,
  Friday = 5,
  Saturday = 6,
}
